import { Soon } from '@/components/soon'

export default function TasksPage() {
  return (
    <Soon
      glyph="•"
      title="Tasks"
      body="Coming next — a calm, status-grouped list, with migrated treated as a first-class state. Reading, not Jira."
    />
  )
}
