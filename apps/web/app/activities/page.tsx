import { Soon } from '@/components/soon'

export default function ActivitiesPage() {
  return (
    <Soon
      glyph="○"
      title="Activities"
      body="Coming next — a filterable log that gently surfaces unlinked activities as candidates for new trackers."
    />
  )
}
