/**
 * @bullet/db — Drizzle schema + migrations + repositories.
 *
 * Repositories (typed functions) are the only way to touch the DB. Depends on @bullet/core.
 */
export const PACKAGE_NAME = '@bullet/db'
