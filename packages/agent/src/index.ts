/**
 * @bullet/agent — "the brain".
 *
 * Ollama client, extraction → resolution → suggestion pipeline, and the serial inference
 * queue. Depends on @bullet/core + @bullet/db. The Ollama client is mockable for tests.
 */
export const PACKAGE_NAME = '@bullet/agent'
