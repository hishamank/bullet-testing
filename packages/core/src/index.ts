/**
 * @bullet/core — domain types, Zod schemas, apply/commit engine, create-vs-append logic.
 *
 * Pure types/schemas + small helpers only. No DB, no IO, no network imports.
 */
export const PACKAGE_NAME = '@bullet/core'
